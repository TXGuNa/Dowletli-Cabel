# SPA routing is handled by wrangler.jsonc ("not_found_handling": "single-page-application").
# No redirect rules here — a "/* /index.html 200" rule causes an infinite-loop
# error on Cloudflare Workers static assets (code 100324).
